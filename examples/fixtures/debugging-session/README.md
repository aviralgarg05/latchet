# Debugging Session Fixture

This fixture models a common coding-agent debugging loop:

- one accepted decision
- one failed attempt
- one open failure
- one environment quirk
- one artifact reference
- one high-priority next action

Use it to validate projection, markdown rendering, import/export, and adapter prompt generation.
