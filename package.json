{
  "name": "latchet",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first task ledger for AI-assisted work.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "find packages -type d \\( -name dist -o -name .tsbuildinfo \\) -prune -exec rm -rf {} + 2>/dev/null || true",
    "test": "npm run build && node --test packages/core/dist/test/*.test.js",
    "cli": "node packages/cli/dist/index.js",
    "mcp": "node packages/mcp/dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
